/commit-and-push

Goal:
Stage all current changes, create a clear commit message, and push to GitHub.

Steps:
1) Run: git add .
2) Generate a concise commit message describing what changed.
3) Run: git commit -m "<AI message>"
4) Run: git push
