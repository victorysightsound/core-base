# /bridge – Native Bridge
Small bits of native code that let the web UI talk to the operating system (file pickers, notifications, secure storage).
Never put business logic here; only OS helpers that are allow-listed and validated.
